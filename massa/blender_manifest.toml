schema_version = "1.0.0"

id = "massa_mesh_gen"
version = "2.0.0"
name = "Massa Mesh Generator"
tagline = "Procedural Geometry Engine with BMesh Cartridges"
maintainer = "3D_Massa"
type = "add-on"


# --- COMPATIBILITY ---
blender_version_min = "5.0.0"

# --- LICENSE ---
license = ["SPDX:GPL-3.0-or-later"]


# --- PERMISSIONS ---
[permissions]
files = "Required for baking and debug logging."

[build]
paths_exclude_pattern = ["__pycache__/*", "/.git/*", "/*.zip"]
